console.groupCollapsed(`slide 03.2`);

// JavaScript Types: String
console.title(`Strings`)
const aString1 = "you could use double-quotes";
const aString2 = 'You could use single-quotes';
const aString3 = `You could use back-ticks`; // <-- This recommended

console.log(`"${aString1}" is a ${typeof aString1}`);
console.log(`"${aString2}" is a ${typeof aString2}`);
console.log(`"${aString3}" is a ${typeof aString3}`);

// P.S.: `typeof` returns the type of the value held by the variable
// Remember: we can modify variables, giving them values of different types

console.groupEnd(`slide 03.2`);
