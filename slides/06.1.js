console.groupCollapsed(`slide 06.1`);

// String like any type, has built-in properties and methods.
const helloWorldString = "Hello World";


// string length (number of characters)
const helloWorldStringLength = helloWorldString.length;
console.log(`The number of characters in "${helloWorldString}" is ${helloWorldStringLength}`);

// Reference: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/length

console.groupEnd(`slide 06.1`);
