console.groupCollapsed(`slide 06.2`)

console.red(`string.charAt(index)`);
// string.charAt(index)
// Returns the character at the specified index.
console.log(`The first character in "${helloWorldString}" is ${helloWorldString.charAt(0)}`);
console.log(`The second character in "${helloWorldString}" is ${helloWorldString.charAt(1)}`);

// Reference: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/charAt

console.groupEnd(`slide 06.2`)